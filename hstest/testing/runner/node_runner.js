const {TestRunner} = require("./runner.js")
const {TestRun} = require("../test_run.js")

class NodeRunner extends TestRunner {   

    executeFunction() {

    }

    test(testRun) {

    }
}

module.exports = {
    NodeRunner: NodeRunner
}
